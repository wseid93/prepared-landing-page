{
  "name": "prepared-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "minify-css": "cleancss -o styles.min.css styles.css",
    "build": "npm run minify-css",
    "optimize": "npm run build"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.2"
  }
}
